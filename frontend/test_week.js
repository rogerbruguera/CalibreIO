const getWeekNumber = (dString) => { const d = new Date(dString); d.setUTCDate(d.getUTCDate() + 4 - (d.getUTCDay() || 7)); const yearStart = new Date(Date.UTC(d.getUTCFullYear(), 0, 1)); return Math.ceil((((Number(d) - Number(yearStart)) / 86400000) + 1) / 7); }; console.log('week:', getWeekNumber('2026-02-09')); console.log('week 22:', getWeekNumber('2026-02-22'));
